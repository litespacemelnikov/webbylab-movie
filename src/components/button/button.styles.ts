import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    button: {
        width: "100%",
        backgroundColor: "#4EADFE",
        padding: 15,
        borderRadius: 200,
    },
    label: {
        color: 'white',
        fontWeight: '500',
        textAlign: 'center',
    }
});