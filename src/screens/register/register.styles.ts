import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    register: {
        marginTop: 20
    },
    description: {
        opacity: 0.6
    },
    form: {
        width: '100%',
        marginTop: 20
    },
});